# Infrastructure boundary

This directory is reserved for future environment-specific infrastructure configuration. Phase 0
uses Docker Compose at the repository root and intentionally selects no cloud provider, deployment
platform, infrastructure-as-code framework, or external monitoring vendor.

Any future infrastructure choice must follow the deployment architecture and be recorded in an ADR.

