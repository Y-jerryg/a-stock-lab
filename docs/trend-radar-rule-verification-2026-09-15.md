# 全市场下降趋势规则验证（2026-09-15）

> 本文记录规则修改完成时的状态。随后已核实用户的全市场扫描完成，得到 1,270 只候选；
> 当前修复和部署状态请参阅 [两个雷达修复验收记录](radar-repair-2026-09-15.md)，
> 操作方法见 [详细使用指南](user-guide.html)。下方“尚未推送”等表述仅描述当时状态。

本次按用户要求修改为全部 A 股扫描，关注度前 300 仅优先展示和高亮，取消默认单日反弹
1.5% 限制。最新 9→8→7 个交易日最多允许 2 天反弹，其余收盘严格下降，最后收盘严格
低于区间内所有此前收盘；保留负斜率、交易日连续性和成交量资料有效性检查。
决策记录见 [ADR 0021](adr/0021-all-a-share-closing-low-trends.md)。

## 已完成

- 后端 230 项非 PostgreSQL 测试通过，前端 29 项测试通过。
- 在新建的临时 PostgreSQL 数据库中，迁移、锁、持久化、共享输出版本 3 和静态详情导出
  集成测试通过；测试结束删除的是临时数据库，不影响原始扫描数据。
- 后端 Ruff、Mypy；前端 ESLint、TypeScript、Prettier 检查通过。
- Python 3.12 / Node 22 Docker 构建成功，本机 backend 和 frontend 已更新且健康。
- 运行中的新镜像实际读取参数为 rule_version=2、all_a、最多 2 天反弹、反弹幅度上限 null。
- 新适配器按默认配置实际取得 5,562 只沪深北 A 股名单，包含用户提及的云鼎科技；一次
  名单请求约 13.5 秒。此耗时不代表全市场日线扫描耗时。
- 对用户指出的股票单独获取日线，按截至 2026-09-11 的序列复核，新规则返回 9 日、
  2 次反弹；加回旧的 1.5% 限制则不入选。原始行情仅保存在忽略的 runtime/qa 中。
- 单元测试覆盖：区间终点不是最低、并列最低、平盘、反弹超过 2 天、超过旧幅度限制、
  最长 9→8→7 窗口、截至最新一根日线、缺少热度仍扫描、非热门候选保留、热门排序优先
  于缩量排序、历史规则说明及新参数读取。

## 当前数据与部署边界

未在本次检查中完成一次 5,562 只日线的全市场扫描；现有正式历史结果及其 18 只候选保留。
重新打开 Start-TrendRadar.cmd 点击“开始抓取”会运行新版本全市场规则，完成后本机页面
读取新结果。“重新导出结果”只修复输出文件，不会重算旧扫描。

本次代码尚未提交或推送到 GitHub，公网网页仍使用已部署的版本。发布新规则数据前，应先
将本次代码同步到 GitHub 并部署更新前端，因为新参数允许 max_single_pullback_pct=null，
旧前端的参数检查不兼容。随后通过电脑窗口“发布网站”上传新结果。
本地价格筛选的修改不涉及付费 AI 调用或改变公网隧道设置。
