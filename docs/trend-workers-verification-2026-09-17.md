# 常驻进程与 4 路并发验证（2026-09-17）

## 已取消的任务

按用户要求停止当时唯一运行的扫描容器，并在取得扫描锁后删除任务
`f0519752-7cef-4403-a2e6-71b951067af5`。删除该次 14 条候选、5,194 条关注度记录、
779 条本次新获取的缓存观察和任务记录。按该次起止时间及行情日期限定缓存删除范围。
原有 3,754 条历史候选记录完整保留，重新导出索引后不再显示该次未完成任务。
完成后复核：该任务记录为 0，运行中的正式扫描为 0。未自动开启新一轮全市场扫描。

## 实测

Docker 内，同一批 24 只股票、同一新浪前复权数据源、同一日期区间
2026-09-11 至 2026-09-16，仅测速抓取，不创建正式扫描、不覆盖公开结果。

| 方法 | 耗时 | 成功 / 最终失败 |
| --- | ---: | ---: |
| 旧方式：串行、每次新建 Python 进程 | 65.372 秒 | 24 / 0 |
| 常驻进程、单路 | 15.424 秒 | 24 / 0 |
| 常驻进程、4 路，正式三次尝试配置复测 | 9.348 秒 | 24 / 0 |

首次 4 路的压力探测只允许单次尝试，15.406 秒内成功 20、失败 4；三种模式共有的
20 只行情数值一致。上表 4 路一行是之后使用三次尝试配置的复测，不能当作多轮平均值。

扩大到 100 只，4 路常驻进程耗时 65.019 秒、100 成功、0 最终失败。期间记录 10 次
SSL 异常和 1 次请求超时，通过重试及替换超时进程恢复。验证使用 20 秒超时，产品默认
仍为 60 秒；该样本时长包含实际触发的重试，不代表全市场默认配置的耗时。

样本中 4 路相对旧方式约 7 倍，但不承诺全市场按此倍数缩短：实际还受补缓存、复权
变化、源切换、接口限流、网络、数据库写入和最终导出影响。24/100 只样本均包含进程
启动与结束开销。测试原始行情及日志仅留在忽略的 runtime/qa 中，不提交到 Git。

## 自动化与集成检查

- 本机非 PostgreSQL 完整回归 253 项通过；随后新增的共享节流、单一恢复探测和资源
  释放检查连同进程/适配器测试共 21 项通过。
- 真实独立测试 PostgreSQL 数据库的扫描、缓存、历史证据、导出集成检查通过。
- 真实子进程测试涵盖进程复用、4 路消息隔离、异常退出、硬超时杀进程、下次请求重建、
  定期回收、结束释放；并发扫描与串行的候选内容一致。
- 验证最多预取 4 只，达到全局失败阈值后停止补队列，保留此前已保存的候选。
- Ruff、Windows/Linux Mypy 与 PowerShell 控制窗口脚本语法检查通过。
- 新 Docker 镜像确认默认 4 路、共享 0.3 秒请求间隔、最多 3 次尝试。
- 本机健康接口及公网尾盘接口返回 200，既有结果可读。

## 使用

关闭旧控制窗口，重新打开 `Start-TrendRadar.cmd`，点击“开始抓取”。无需另开多个窗口。
本机镜像已更新；另一台电脑拉取代码后先点“准备本地环境”。缓存、筛选和发布步骤不变。
见 [完整指南](user-guide.html) 和 [ADR 0025](adr/0025-reusable-trend-provider-workers.md)。
