# 两个雷达修复验收记录（2026-09-15）

日常操作请阅读 [完整使用指南](user-guide.html)，或双击项目根目录的 `Start-Guide.cmd`。

## 发现的问题

- 用户夜间趋势扫描在北京时间 9 月 14 日 23:23 开始、15 日 04:18 结束，约 4 小时 55 分。
  数据库状态为 `completed_with_warnings`：请求 5,562、处理成功 5,549、失败 13、候选 1,270、
  强缩量 117。不是全部扫描失败，既有成功数据完整保留。
- 13 个失败中，12 个缺少目标交易日日线，1 个行情接口失败。缺少日线不等于已确认停牌。
- 已上传的趋势数据使用全市场规则和空反弹幅度上限，公网旧前端拒绝这种配置；
  本机列表还会重新按成交量排序，打乱关注度前 300 优先顺序。
- 公网尾盘网关及隧道容器处于停止状态，网页不能连接本机 API。
- 尾盘 9 月 14 日快照成功，380 个候选中的 68 个分时技术分析曾因数据源失败；
  AI 研究待处理表示尚未发起按需研究，不表示快照失败。
- 旧电脑窗口只使用进程退出状态，未提供明确的业务完成摘要。尚无足够证据将用户看到
  的“失败”精确归因于某一个 Windows 退出码问题。

## 已完成的修复

- 保留全市场扫描、最新 7～9 个收盘、最多 2 天反弹、末日严格最低、取消默认 1.5% 上限。
- 新前端兼容新旧规则，保留发布时的热门优先顺序，增加全结果搜索及每页 30 个候选。
- 命令行提供紧凑扫描摘要；Windows 操作脚本向控制窗口写入明确结果文件，区分已完成、
  部分失败但可用、真正失败和发布已提交。正常日志不再仅凭 stderr 判作业务失败。
- 修复 Windows PATH 存在多个 GitHub CLI 入口时的解析问题。
- 发布前检查 GitHub 上的网页规则版本，发现旧网页时先提示更新代码，不盲目上传不兼容结果。
- 控制窗口增加本机启动、运行状态、公网开关、尾盘入口及离线指南。
- 已恢复既有公网连接；已针对原尾盘工作流执行补抓，380 个技术分析全部成功，
  原始快照及历史时点保持不变，没有触发付费 AI。

## 自动化与本机验收

- 后端 Ruff、Mypy 通过；非 PostgreSQL 测试 234 项通过。
- 前端 ESLint、TypeScript 通过；测试 31 项通过，覆盖热门排序、新参数、搜索分页和详情。
- Windows 脚本真实子进程回归覆盖扫描成功/部分失败/失败、GitHub 发布及版本兼容检查。
- Docker 后端和前端构建成功；实际执行“启动本机服务”和“检查运行状态”均成功。
- 本机查询尾盘为 9 月 14 日、380 个候选；趋势为同日、1,270 个候选。
- 当前默认启用尾盘定时 worker；趋势以手动抓取为准。公网趋势更新需另外发布。
- 本次复用用户已经完成的全市场扫描，没有重复执行另一次约 5 小时扫描。
- 未使用真实付费 API Key 执行 AI 研究；账户余额、模型权限及上游网络需由实际调用确认。

## 9 月 16 日最终部署与追加需求验收

- 昨日 Windows “发布网站”实际执行结果为 exit_code=0、state=submitted；对应 Pages 部署
  [34915357447](https://github.com/Y-jerryg/a-stock-lab/actions/runs/34915357447) 成功。
- 按追加要求，新尾盘筛选为 `tail-radar-screen-v2`、3%～5%（含两端）；历史 v1 仍按原
  2%～3% 读取。已更新运行中的 backend 和 worker，下一次正式快照使用新规则。
  9 月 14 日历史候选未覆盖，也未用 9 月 16 日行情伪造过去快照。
- 尾盘详情改为保存的 5 分钟 K 线及成交量，联动缩放，悬停显示 OHLC、股数和成交额。
  本机浏览器实际打开一汽解放详情，42 根有效历史 bar 的蜡烛图及成交量图显示正常。
- 电脑重启后测试隧道换址，已重新连接并更新 Pages 的后端地址。公网尾盘最新记录与
  单股详情 HTTP 200，跨域允许正确的 Pages 来源；AI 预检 204，无 Key 请求 401。
- 新版 Pages 部署 [35057581487](https://github.com/Y-jerryg/a-stock-lab/actions/runs/35057581487)
  成功，提交 `0e2c21b`。公网 HTML 引用的新详情模块和 K 线模块均读取成功。
- 公网趋势 index 显示 1,270 个候选，详情包读取成功且有 1,270 个股票条目；示例保存 59 根日线。
- 本机后端非 PostgreSQL 测试 235 项、前端 33 项通过；新版完整
  [CI 35057581474](https://github.com/Y-jerryg/a-stock-lab/actions/runs/35057581474) 成功，
  包括 PostgreSQL 迁移/集成测试、格式、lint、类型检查及两端构建。
- 内置浏览器访问公网 Pages 多次超时，公网验收采用真实 HTTPS 响应、模块及数据读取和
  部署状态；本机图表完成浏览器目视验证。没有将公网浏览器交互或真实付费 AI 调用记作已验证。

使用入口：双击 `Start-TrendRadar.cmd` 打开电脑控制窗口；双击 `Start-Guide.cmd` 打开
[离线完整指南](user-guide.html)。不需要重新执行已经完成的全市场扫描来查看或发布旧结果。
